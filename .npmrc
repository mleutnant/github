# onnxruntime-node (transitive dep of hyperframes) defaults to fetching CUDA
# binaries from api.nuget.org on linux/x64 — unnecessary for this project's
# CPU-only FFmpeg/Puppeteer render pipeline, and blocks in restricted network
# environments. Skip it.
onnxruntime-node-install=skip
