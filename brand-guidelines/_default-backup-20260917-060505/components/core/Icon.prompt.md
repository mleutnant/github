One icon from the brand's own 16-glyph set — line icons with rounded joins and deliberate breaks in the stroke.

```jsx
<Icon name="barrierefrei" size={56} />
<Icon name="sicherheit" size={56} tone="white" />
```

Tones: `ink` (black on white), `white` (on blue), `red` (website only). Never scale below 32px; never recolour to a non-brand colour. Pair with a caption via `FeatureIcon`.
