Website masthead. Nav labels are bold, blue, and turn red on hover and for the active page.

```jsx
<SiteHeader active="produkte" onNavigate={setPage}
  items={[{id:'produkte',label:'Produkte'},{id:'kunde',label:'Kunde'},{id:'referenzen',label:'Referenzen'}]} />
```
