Radio set for forms such as "in gedruckter Form / als PDF-Datei".

```jsx
<RadioGroup name="format" legend="Format" inline value={v} onChange={e=>setV(e.target.value)}
  options={['gedruckt','PDF-Datei']} />
```
