Two stacked yellow squares pinned to the right edge of a web page — contact and back-to-top.

```jsx
<SideRail onContact={() => setPage('kontakt')} onTop={() => scrollTo(0,0)} />
```

The back-to-top chevron is drawn from two borders; the contact glyph is the brand's own "beratung" icon.
